<h1>Pours</h1>
<ul class="pours">
{{#each}}
    <li>
      <span class="brewery">{{brewery}}</span>
      <span class="beerName">{{beerName}}</span>
      <span class="venue">at {{venue}}</span>
      <span class="beerRating"> {{ratingDescriptor}}</span>
    </li>
{{/each}}
</ul>
